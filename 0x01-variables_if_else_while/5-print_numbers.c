#include <stdio.h>

/**
 *main - Entry point
 *
 * Description: A C program that prints with put function
 *
 * Return: Alawys 0 (Success)
*/

int main(void)
{
	int a;

	for (a = 0; a <= 9; a++)
		printf("%d", a);
	printf("\n");
	return (0);
}
